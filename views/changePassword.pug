extends layouts/main

block content
    .form-container 
        form(action=routes.changePassword, method="post")
            input(type="password", placeholder="Current Password", name="cPassword")
            input(type="password", placeholder="New Password", name="nPassword")
            input(type="password", placeholder="Verify New Password", name="vPassword")
            input(type="submit", value="change Password")
        